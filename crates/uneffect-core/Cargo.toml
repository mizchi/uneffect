[package]
name = "uneffect-core"
version = "0.1.0"
edition = "2024"
description = "Native neutral IR and capability lattice for Uneffect"
license = "MIT"
repository = "https://github.com/mizchi/neo-effect"
readme = "README.md"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
