# uneffect-core

Rust neutral IR, Corsa frontend interchange schema, structured capability lattice, and ownership/invalidation checks for Uneffect.

The public Corsa interchange schema is versioned by `CORSA_FRONTEND_SCHEMA_VERSION`. Unknown versions are rejected rather than interpreted optimistically.
