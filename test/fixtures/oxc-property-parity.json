[
  {
    "options": {
      "files": {
        "scalar.ts": "/* uneffect:requires x >= 0 */\n/* uneffect:ensures result >= x */\nexport function step(x: Nat, y: U8 = 1): Nat { return x + y; }"
      },
      "cases": 7,
      "seed": 31
    },
    "expected": {
      "generatedFiles": {
        "scalar.uneffect.test.ts": "// Generated by Uneffect. Test-only code; no production runtime dependency.\nimport { expect, test } from \"vitest\"\nimport { step } from \"./scalar.js\"\n\nconst domains = [\"Nat\",\"U8\"] as const\nconst refinementValues = [[0,1],[]] as const\nconst refinementTuples = [] as const\nconst limits: Record<string, readonly number[]> = {\"Int\":[0,1,-1,2,-2,2147483647,-2147483648],\"Nat\":[0,1,2,255,65535],\"U8\":[0,1,2,254,255],\"U32\":[0,1,2,4294967294,4294967295],\"I32\":[0,1,-1,2147483647,-2147483648]}\nconst replay = undefined\ntype Domain = typeof domains[number]\nfunction values(domain: any, refined: readonly any[] = []): any[] { if (typeof domain === \"string\") return [...new Set([...refined, ...limits[domain]!] as any[])]; if (domain.kind === \"union\") return [...new Set([...refined, ...domain.members.flatMap((member: any) => typeof member === \"string\" ? limits[member]! : [member.value])] as any[])]; if (domain.kind === \"specialized\") return [...domain.values]; if (domain.kind === \"record\") { let out: any[] = [{}]; for (const [name, field] of Object.entries(domain.fields)) { const fieldValues = [...(domain.optional?.includes(name) ? [undefined] : []), ...values(field).slice(0, 5)]; out = out.flatMap(record => fieldValues.map(value => value === undefined ? record : ({ ...record, [name]: value }))).slice(0, 64) } return out } if (domain.kind === \"bounded-set\") { const edges = limits[domain.element]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, edges.length) + 1 }, (_, length) => edges.slice(0, length)) } if (domain.kind === \"bounded-map\") { const keys = limits[domain.key]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, keys.length) + 1 }, (_, length) => ({ keys: keys.slice(0, length), values: keys.slice(0, length).map((_: number, index: number) => limits[domain.value]![index % limits[domain.value]!.length]!) })) } const maximum = Math.min(domain.maximum, 4096); const lengths = [...new Set([0, Math.min(1, maximum), Math.min(2, maximum), maximum])]; return lengths.map((length, offset) => Array.from({ length }, (_, index) => limits[domain.element]![(index + offset) % limits[domain.element]!.length]!)) }\nfunction shrinkNumber(value: number, domain: string): number[] { const values: number[] = []; let current = value; while (Math.abs(current) > 1) { current = Math.trunc(current / 2); values.push(current) } values.push(0); return [...new Set(values)].filter(value => ![\"Nat\", \"U8\", \"U32\"].includes(domain) || value >= 0) }\nfunction shrink(value: any, domain: any): any[] { if (typeof domain === \"string\") return typeof value === \"number\" ? shrinkNumber(value, domain) : []; if (domain.kind === \"union\") return domain.members.flatMap((member: any) => typeof member === \"string\" && typeof value === \"number\" ? shrinkNumber(value, member) : []); if (domain.kind === \"specialized\") return domain.values.filter((candidate: any) => candidate !== value).sort((left: any, right: any) => sampleSize([left]) - sampleSize([right])); if (domain.kind === \"record\") return Object.entries(domain.fields).flatMap(([name, field]) => { const omitted = domain.optional?.includes(name) && Object.hasOwn(value, name) ? [Object.fromEntries(Object.entries(value).filter(([key]) => key !== name))] : []; return [...omitted, ...shrink(value[name], field).map(candidate => ({ ...value, [name]: candidate }))] }); if (domain.kind === \"bounded-set\") return [...value.map((_: any, index: number) => value.filter((__: any, at: number) => at !== index)), ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).filter((candidate: number) => !value.some((other: number, at: number) => at !== index && other === candidate)).map((candidate: number) => value.with(index, candidate))), []]; if (domain.kind === \"bounded-map\") return [...value.keys.map((_: any, index: number) => ({ keys: value.keys.filter((__: any, at: number) => at !== index), values: value.values.filter((__: any, at: number) => at !== index) })), ...value.values.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.value).map(candidate => ({ keys: value.keys, values: value.values.with(index, candidate) }))), { keys: [], values: [] }]; const structural: number[][] = []; for (let length = Math.floor(value.length / 2); length > 0; length = Math.floor(length / 2)) structural.push(value.slice(0, length)); if (value.length > 1) structural.push(value.slice(0, 1)); return [...structural, ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).map(candidate => value.with(index, candidate))), []] }\nfunction materialize(value: any, domain: Domain): any { if (typeof domain === \"object\" && domain.kind === \"bounded-array\") return domain.element === \"U8\" ? new Uint8Array(value) : new Uint32Array(value); if (typeof domain === \"object\" && domain.kind === \"bounded-set\") return new Set(value); if (typeof domain === \"object\" && domain.kind === \"bounded-map\") return new Map(value.keys.map((key: number, index: number) => [key, value.values[index]])); return value }\nfunction sampleSize(values: readonly any[]): number { const size = (value: any): number => Array.isArray(value) ? value.length + value.reduce((sum: number, entry: any) => sum + size(entry), 0) : value !== null && typeof value === \"object\" ? Object.values(value).reduce((sum: number, entry) => sum + size(entry), 0) : typeof value === \"number\" ? Math.abs(value) : typeof value === \"string\" ? value.length : Number(value); return values.reduce((total, value) => total + size(value), 0) }\nfunction random(seed: number) { let state = seed >>> 0; return () => ((state = (Math.imul(state, 1664525) + 1013904223) >>> 0) / 0x100000000) }\nfunction samples() { const out: any[][] = replay?.functionName === \"step\" && Array.isArray(replay.arguments) ? [[...replay.arguments]] : []; for (const row of refinementTuples) if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push([...row]); const limit = Math.max(7, out.length); const visit = (at: number, row: any[]) => { if (out.length >= limit) return; if (at === domains.length) { if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push(row); return } for (const value of values(domains[at]!, refinementValues[at])) visit(at + 1, [...row, value]) }; visit(0, []); const next = random(31); while (out.length < limit) out.push(domains.map((domain, index) => { const candidates = values(domain, refinementValues[index]); return candidates[Math.floor(next() * candidates.length)]! })); return out }\nconst precondition = (x, y) => (x >= 0)\nconst property = (x, y) => { const result = step(x, y); return (result >= x) }\n\ntest(\"uneffect property: step\", () => {\n  for (const candidate of samples()) {\n    const invoke = (values: any[]) => values.map((value, index) => materialize(value, domains[index]!))\n    if (!precondition(...invoke(candidate))) continue\n    if (property(...invoke(candidate))) continue\n    const minimal = [...candidate]; for (const joint of refinementTuples.map(row => [...row]).sort((left, right) => sampleSize(left) - sampleSize(right))) { if (sampleSize(joint) < sampleSize(minimal) && precondition(...invoke(joint)) && !property(...invoke(joint))) minimal.splice(0, minimal.length, ...joint) } for (let index = 0; index < minimal.length; index++) for (const value of shrink(minimal[index]!, domains[index]!)) { const next = minimal.with(index, value); if (precondition(...invoke(next)) && !property(...invoke(next))) minimal[index] = value }\n    expect.fail(\"Uneffect counterexample: \" + JSON.stringify({ functionName: \"step\", arguments: minimal, replayed: Boolean(replay && JSON.stringify(candidate) === JSON.stringify(replay.arguments)) }))\n  }\n})\n"
      },
      "boundaries": [
        {
          "fileName": "scalar.ts",
          "functionName": "step",
          "generators": [
            "Nat",
            "U8"
          ],
          "shrinkers": [
            "Nat",
            "U8"
          ],
          "generatorHints": [
            [
              0,
              1
            ],
            []
          ],
          "generatorTuples": [],
          "requires": [
            "x >= 0"
          ],
          "ensures": [
            "result >= x"
          ]
        }
      ],
      "diagnostics": []
    }
  },
  {
    "options": {
      "files": {
        "record.ts": "/* uneffect:requires p.x >= 1 && p.child !== undefined */\n/* uneffect:ensures result === p.x */\nexport function read(p: { x: Int; child?: { y: Nat } }): Int { return p.x; }"
      }
    },
    "expected": {
      "generatedFiles": {
        "record.uneffect.test.ts": "// Generated by Uneffect. Test-only code; no production runtime dependency.\nimport { expect, test } from \"vitest\"\nimport { read } from \"./record.js\"\n\nconst domains = [{\"kind\":\"record\",\"fields\":{\"x\":\"Int\",\"child\":{\"kind\":\"record\",\"fields\":{\"y\":\"Nat\"}}},\"optional\":[\"child\"]}] as const\nconst refinementValues = [[]] as const\nconst refinementTuples = [] as const\nconst limits: Record<string, readonly number[]> = {\"Int\":[0,1,-1,2,-2,2147483647,-2147483648],\"Nat\":[0,1,2,255,65535],\"U8\":[0,1,2,254,255],\"U32\":[0,1,2,4294967294,4294967295],\"I32\":[0,1,-1,2147483647,-2147483648]}\nconst replay = undefined\ntype Domain = typeof domains[number]\nfunction values(domain: any, refined: readonly any[] = []): any[] { if (typeof domain === \"string\") return [...new Set([...refined, ...limits[domain]!] as any[])]; if (domain.kind === \"union\") return [...new Set([...refined, ...domain.members.flatMap((member: any) => typeof member === \"string\" ? limits[member]! : [member.value])] as any[])]; if (domain.kind === \"specialized\") return [...domain.values]; if (domain.kind === \"record\") { let out: any[] = [{}]; for (const [name, field] of Object.entries(domain.fields)) { const fieldValues = [...(domain.optional?.includes(name) ? [undefined] : []), ...values(field).slice(0, 5)]; out = out.flatMap(record => fieldValues.map(value => value === undefined ? record : ({ ...record, [name]: value }))).slice(0, 64) } return out } if (domain.kind === \"bounded-set\") { const edges = limits[domain.element]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, edges.length) + 1 }, (_, length) => edges.slice(0, length)) } if (domain.kind === \"bounded-map\") { const keys = limits[domain.key]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, keys.length) + 1 }, (_, length) => ({ keys: keys.slice(0, length), values: keys.slice(0, length).map((_: number, index: number) => limits[domain.value]![index % limits[domain.value]!.length]!) })) } const maximum = Math.min(domain.maximum, 4096); const lengths = [...new Set([0, Math.min(1, maximum), Math.min(2, maximum), maximum])]; return lengths.map((length, offset) => Array.from({ length }, (_, index) => limits[domain.element]![(index + offset) % limits[domain.element]!.length]!)) }\nfunction shrinkNumber(value: number, domain: string): number[] { const values: number[] = []; let current = value; while (Math.abs(current) > 1) { current = Math.trunc(current / 2); values.push(current) } values.push(0); return [...new Set(values)].filter(value => ![\"Nat\", \"U8\", \"U32\"].includes(domain) || value >= 0) }\nfunction shrink(value: any, domain: any): any[] { if (typeof domain === \"string\") return typeof value === \"number\" ? shrinkNumber(value, domain) : []; if (domain.kind === \"union\") return domain.members.flatMap((member: any) => typeof member === \"string\" && typeof value === \"number\" ? shrinkNumber(value, member) : []); if (domain.kind === \"specialized\") return domain.values.filter((candidate: any) => candidate !== value).sort((left: any, right: any) => sampleSize([left]) - sampleSize([right])); if (domain.kind === \"record\") return Object.entries(domain.fields).flatMap(([name, field]) => { const omitted = domain.optional?.includes(name) && Object.hasOwn(value, name) ? [Object.fromEntries(Object.entries(value).filter(([key]) => key !== name))] : []; return [...omitted, ...shrink(value[name], field).map(candidate => ({ ...value, [name]: candidate }))] }); if (domain.kind === \"bounded-set\") return [...value.map((_: any, index: number) => value.filter((__: any, at: number) => at !== index)), ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).filter((candidate: number) => !value.some((other: number, at: number) => at !== index && other === candidate)).map((candidate: number) => value.with(index, candidate))), []]; if (domain.kind === \"bounded-map\") return [...value.keys.map((_: any, index: number) => ({ keys: value.keys.filter((__: any, at: number) => at !== index), values: value.values.filter((__: any, at: number) => at !== index) })), ...value.values.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.value).map(candidate => ({ keys: value.keys, values: value.values.with(index, candidate) }))), { keys: [], values: [] }]; const structural: number[][] = []; for (let length = Math.floor(value.length / 2); length > 0; length = Math.floor(length / 2)) structural.push(value.slice(0, length)); if (value.length > 1) structural.push(value.slice(0, 1)); return [...structural, ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).map(candidate => value.with(index, candidate))), []] }\nfunction materialize(value: any, domain: Domain): any { if (typeof domain === \"object\" && domain.kind === \"bounded-array\") return domain.element === \"U8\" ? new Uint8Array(value) : new Uint32Array(value); if (typeof domain === \"object\" && domain.kind === \"bounded-set\") return new Set(value); if (typeof domain === \"object\" && domain.kind === \"bounded-map\") return new Map(value.keys.map((key: number, index: number) => [key, value.values[index]])); return value }\nfunction sampleSize(values: readonly any[]): number { const size = (value: any): number => Array.isArray(value) ? value.length + value.reduce((sum: number, entry: any) => sum + size(entry), 0) : value !== null && typeof value === \"object\" ? Object.values(value).reduce((sum: number, entry) => sum + size(entry), 0) : typeof value === \"number\" ? Math.abs(value) : typeof value === \"string\" ? value.length : Number(value); return values.reduce((total, value) => total + size(value), 0) }\nfunction random(seed: number) { let state = seed >>> 0; return () => ((state = (Math.imul(state, 1664525) + 1013904223) >>> 0) / 0x100000000) }\nfunction samples() { const out: any[][] = replay?.functionName === \"read\" && Array.isArray(replay.arguments) ? [[...replay.arguments]] : []; for (const row of refinementTuples) if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push([...row]); const limit = Math.max(100, out.length); const visit = (at: number, row: any[]) => { if (out.length >= limit) return; if (at === domains.length) { if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push(row); return } for (const value of values(domains[at]!, refinementValues[at])) visit(at + 1, [...row, value]) }; visit(0, []); const next = random(24301); while (out.length < limit) out.push(domains.map((domain, index) => { const candidates = values(domain, refinementValues[index]); return candidates[Math.floor(next() * candidates.length)]! })); return out }\nconst precondition = (p) => (p.x >= 1 && p.child !== undefined)\nconst property = (p) => { const result = read(p); return (result === p.x) }\n\ntest(\"uneffect property: read\", () => {\n  for (const candidate of samples()) {\n    const invoke = (values: any[]) => values.map((value, index) => materialize(value, domains[index]!))\n    if (!precondition(...invoke(candidate))) continue\n    if (property(...invoke(candidate))) continue\n    const minimal = [...candidate]; for (const joint of refinementTuples.map(row => [...row]).sort((left, right) => sampleSize(left) - sampleSize(right))) { if (sampleSize(joint) < sampleSize(minimal) && precondition(...invoke(joint)) && !property(...invoke(joint))) minimal.splice(0, minimal.length, ...joint) } for (let index = 0; index < minimal.length; index++) for (const value of shrink(minimal[index]!, domains[index]!)) { const next = minimal.with(index, value); if (precondition(...invoke(next)) && !property(...invoke(next))) minimal[index] = value }\n    expect.fail(\"Uneffect counterexample: \" + JSON.stringify({ functionName: \"read\", arguments: minimal, replayed: Boolean(replay && JSON.stringify(candidate) === JSON.stringify(replay.arguments)) }))\n  }\n})\n"
      },
      "boundaries": [
        {
          "fileName": "record.ts",
          "functionName": "read",
          "generators": [
            {
              "kind": "record",
              "fields": {
                "x": "Int",
                "child": {
                  "kind": "record",
                  "fields": {
                    "y": "Nat"
                  }
                }
              },
              "optional": [
                "child"
              ]
            }
          ],
          "shrinkers": [
            {
              "kind": "record",
              "fields": {
                "x": "Int",
                "child": {
                  "kind": "record",
                  "fields": {
                    "y": "Nat"
                  }
                }
              },
              "optional": [
                "child"
              ]
            }
          ],
          "generatorHints": [
            []
          ],
          "generatorTuples": [],
          "requires": [
            "p.x >= 1 && p.child !== undefined"
          ],
          "ensures": [
            "result === p.x"
          ]
        }
      ],
      "diagnostics": []
    }
  },
  {
    "options": {
      "files": {
        "collections.ts": "/* uneffect:requires a.length > 0 && s.has(1) && m.has(2) */\n/* uneffect:ensures result === a[0] */\nexport function read(a: BoundedUint8Array<3>, s: BoundedSet<U8, 2>, m: BoundedMap<U8, U32, 2>): number { return a[0]; }"
      }
    },
    "expected": {
      "generatedFiles": {
        "collections.uneffect.test.ts": "// Generated by Uneffect. Test-only code; no production runtime dependency.\nimport { expect, test } from \"vitest\"\nimport { read } from \"./collections.js\"\n\nconst domains = [{\"kind\":\"bounded-array\",\"element\":\"U8\",\"maximum\":3},{\"kind\":\"bounded-set\",\"element\":\"U8\",\"maximum\":2},{\"kind\":\"bounded-map\",\"key\":\"U8\",\"value\":\"U32\",\"maximum\":2}] as const\nconst refinementValues = [[],[],[]] as const\nconst refinementTuples = [] as const\nconst limits: Record<string, readonly number[]> = {\"Int\":[0,1,-1,2,-2,2147483647,-2147483648],\"Nat\":[0,1,2,255,65535],\"U8\":[0,1,2,254,255],\"U32\":[0,1,2,4294967294,4294967295],\"I32\":[0,1,-1,2147483647,-2147483648]}\nconst replay = undefined\ntype Domain = typeof domains[number]\nfunction values(domain: any, refined: readonly any[] = []): any[] { if (typeof domain === \"string\") return [...new Set([...refined, ...limits[domain]!] as any[])]; if (domain.kind === \"union\") return [...new Set([...refined, ...domain.members.flatMap((member: any) => typeof member === \"string\" ? limits[member]! : [member.value])] as any[])]; if (domain.kind === \"specialized\") return [...domain.values]; if (domain.kind === \"record\") { let out: any[] = [{}]; for (const [name, field] of Object.entries(domain.fields)) { const fieldValues = [...(domain.optional?.includes(name) ? [undefined] : []), ...values(field).slice(0, 5)]; out = out.flatMap(record => fieldValues.map(value => value === undefined ? record : ({ ...record, [name]: value }))).slice(0, 64) } return out } if (domain.kind === \"bounded-set\") { const edges = limits[domain.element]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, edges.length) + 1 }, (_, length) => edges.slice(0, length)) } if (domain.kind === \"bounded-map\") { const keys = limits[domain.key]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, keys.length) + 1 }, (_, length) => ({ keys: keys.slice(0, length), values: keys.slice(0, length).map((_: number, index: number) => limits[domain.value]![index % limits[domain.value]!.length]!) })) } const maximum = Math.min(domain.maximum, 4096); const lengths = [...new Set([0, Math.min(1, maximum), Math.min(2, maximum), maximum])]; return lengths.map((length, offset) => Array.from({ length }, (_, index) => limits[domain.element]![(index + offset) % limits[domain.element]!.length]!)) }\nfunction shrinkNumber(value: number, domain: string): number[] { const values: number[] = []; let current = value; while (Math.abs(current) > 1) { current = Math.trunc(current / 2); values.push(current) } values.push(0); return [...new Set(values)].filter(value => ![\"Nat\", \"U8\", \"U32\"].includes(domain) || value >= 0) }\nfunction shrink(value: any, domain: any): any[] { if (typeof domain === \"string\") return typeof value === \"number\" ? shrinkNumber(value, domain) : []; if (domain.kind === \"union\") return domain.members.flatMap((member: any) => typeof member === \"string\" && typeof value === \"number\" ? shrinkNumber(value, member) : []); if (domain.kind === \"specialized\") return domain.values.filter((candidate: any) => candidate !== value).sort((left: any, right: any) => sampleSize([left]) - sampleSize([right])); if (domain.kind === \"record\") return Object.entries(domain.fields).flatMap(([name, field]) => { const omitted = domain.optional?.includes(name) && Object.hasOwn(value, name) ? [Object.fromEntries(Object.entries(value).filter(([key]) => key !== name))] : []; return [...omitted, ...shrink(value[name], field).map(candidate => ({ ...value, [name]: candidate }))] }); if (domain.kind === \"bounded-set\") return [...value.map((_: any, index: number) => value.filter((__: any, at: number) => at !== index)), ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).filter((candidate: number) => !value.some((other: number, at: number) => at !== index && other === candidate)).map((candidate: number) => value.with(index, candidate))), []]; if (domain.kind === \"bounded-map\") return [...value.keys.map((_: any, index: number) => ({ keys: value.keys.filter((__: any, at: number) => at !== index), values: value.values.filter((__: any, at: number) => at !== index) })), ...value.values.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.value).map(candidate => ({ keys: value.keys, values: value.values.with(index, candidate) }))), { keys: [], values: [] }]; const structural: number[][] = []; for (let length = Math.floor(value.length / 2); length > 0; length = Math.floor(length / 2)) structural.push(value.slice(0, length)); if (value.length > 1) structural.push(value.slice(0, 1)); return [...structural, ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).map(candidate => value.with(index, candidate))), []] }\nfunction materialize(value: any, domain: Domain): any { if (typeof domain === \"object\" && domain.kind === \"bounded-array\") return domain.element === \"U8\" ? new Uint8Array(value) : new Uint32Array(value); if (typeof domain === \"object\" && domain.kind === \"bounded-set\") return new Set(value); if (typeof domain === \"object\" && domain.kind === \"bounded-map\") return new Map(value.keys.map((key: number, index: number) => [key, value.values[index]])); return value }\nfunction sampleSize(values: readonly any[]): number { const size = (value: any): number => Array.isArray(value) ? value.length + value.reduce((sum: number, entry: any) => sum + size(entry), 0) : value !== null && typeof value === \"object\" ? Object.values(value).reduce((sum: number, entry) => sum + size(entry), 0) : typeof value === \"number\" ? Math.abs(value) : typeof value === \"string\" ? value.length : Number(value); return values.reduce((total, value) => total + size(value), 0) }\nfunction random(seed: number) { let state = seed >>> 0; return () => ((state = (Math.imul(state, 1664525) + 1013904223) >>> 0) / 0x100000000) }\nfunction samples() { const out: any[][] = replay?.functionName === \"read\" && Array.isArray(replay.arguments) ? [[...replay.arguments]] : []; for (const row of refinementTuples) if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push([...row]); const limit = Math.max(100, out.length); const visit = (at: number, row: any[]) => { if (out.length >= limit) return; if (at === domains.length) { if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push(row); return } for (const value of values(domains[at]!, refinementValues[at])) visit(at + 1, [...row, value]) }; visit(0, []); const next = random(24301); while (out.length < limit) out.push(domains.map((domain, index) => { const candidates = values(domain, refinementValues[index]); return candidates[Math.floor(next() * candidates.length)]! })); return out }\nconst precondition = (a, s, m) => (a.length > 0 && s.has(1) && m.has(2))\nconst property = (a, s, m) => { const result = read(a, s, m); return (result === a[0]) }\n\ntest(\"uneffect property: read\", () => {\n  for (const candidate of samples()) {\n    const invoke = (values: any[]) => values.map((value, index) => materialize(value, domains[index]!))\n    if (!precondition(...invoke(candidate))) continue\n    if (property(...invoke(candidate))) continue\n    const minimal = [...candidate]; for (const joint of refinementTuples.map(row => [...row]).sort((left, right) => sampleSize(left) - sampleSize(right))) { if (sampleSize(joint) < sampleSize(minimal) && precondition(...invoke(joint)) && !property(...invoke(joint))) minimal.splice(0, minimal.length, ...joint) } for (let index = 0; index < minimal.length; index++) for (const value of shrink(minimal[index]!, domains[index]!)) { const next = minimal.with(index, value); if (precondition(...invoke(next)) && !property(...invoke(next))) minimal[index] = value }\n    expect.fail(\"Uneffect counterexample: \" + JSON.stringify({ functionName: \"read\", arguments: minimal, replayed: Boolean(replay && JSON.stringify(candidate) === JSON.stringify(replay.arguments)) }))\n  }\n})\n"
      },
      "boundaries": [
        {
          "fileName": "collections.ts",
          "functionName": "read",
          "generators": [
            {
              "kind": "bounded-array",
              "element": "U8",
              "maximum": 3
            },
            {
              "kind": "bounded-set",
              "element": "U8",
              "maximum": 2
            },
            {
              "kind": "bounded-map",
              "key": "U8",
              "value": "U32",
              "maximum": 2
            }
          ],
          "shrinkers": [
            {
              "kind": "bounded-array",
              "element": "U8",
              "maximum": 3
            },
            {
              "kind": "bounded-set",
              "element": "U8",
              "maximum": 2
            },
            {
              "kind": "bounded-map",
              "key": "U8",
              "value": "U32",
              "maximum": 2
            }
          ],
          "generatorHints": [
            [],
            [],
            []
          ],
          "generatorTuples": [],
          "requires": [
            "a.length > 0 && s.has(1) && m.has(2)"
          ],
          "ensures": [
            "result === a[0]"
          ]
        }
      ],
      "diagnostics": []
    }
  },
  {
    "options": {
      "files": {
        "unions.ts": "/* uneffect:requires x !== -1 && ok === true */\n/* uneffect:ensures result === text */\nexport function read(x: -1 | 0 | 2, ok: true | false, text: \"😀\" | \"good\"): string { return text; }"
      }
    },
    "expected": {
      "generatedFiles": {
        "unions.uneffect.test.ts": "// Generated by Uneffect. Test-only code; no production runtime dependency.\nimport { expect, test } from \"vitest\"\nimport { read } from \"./unions.js\"\n\nconst domains = [{\"kind\":\"union\",\"members\":[{\"kind\":\"literal\",\"value\":-1},{\"kind\":\"literal\",\"value\":0},{\"kind\":\"literal\",\"value\":2}]},{\"kind\":\"union\",\"members\":[{\"kind\":\"literal\",\"value\":true},{\"kind\":\"literal\",\"value\":false}]},{\"kind\":\"union\",\"members\":[{\"kind\":\"literal\",\"value\":\"😀\"},{\"kind\":\"literal\",\"value\":\"good\"}]}] as const\nconst refinementValues = [[],[],[]] as const\nconst refinementTuples = [] as const\nconst limits: Record<string, readonly number[]> = {\"Int\":[0,1,-1,2,-2,2147483647,-2147483648],\"Nat\":[0,1,2,255,65535],\"U8\":[0,1,2,254,255],\"U32\":[0,1,2,4294967294,4294967295],\"I32\":[0,1,-1,2147483647,-2147483648]}\nconst replay = undefined\ntype Domain = typeof domains[number]\nfunction values(domain: any, refined: readonly any[] = []): any[] { if (typeof domain === \"string\") return [...new Set([...refined, ...limits[domain]!] as any[])]; if (domain.kind === \"union\") return [...new Set([...refined, ...domain.members.flatMap((member: any) => typeof member === \"string\" ? limits[member]! : [member.value])] as any[])]; if (domain.kind === \"specialized\") return [...domain.values]; if (domain.kind === \"record\") { let out: any[] = [{}]; for (const [name, field] of Object.entries(domain.fields)) { const fieldValues = [...(domain.optional?.includes(name) ? [undefined] : []), ...values(field).slice(0, 5)]; out = out.flatMap(record => fieldValues.map(value => value === undefined ? record : ({ ...record, [name]: value }))).slice(0, 64) } return out } if (domain.kind === \"bounded-set\") { const edges = limits[domain.element]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, edges.length) + 1 }, (_, length) => edges.slice(0, length)) } if (domain.kind === \"bounded-map\") { const keys = limits[domain.key]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, keys.length) + 1 }, (_, length) => ({ keys: keys.slice(0, length), values: keys.slice(0, length).map((_: number, index: number) => limits[domain.value]![index % limits[domain.value]!.length]!) })) } const maximum = Math.min(domain.maximum, 4096); const lengths = [...new Set([0, Math.min(1, maximum), Math.min(2, maximum), maximum])]; return lengths.map((length, offset) => Array.from({ length }, (_, index) => limits[domain.element]![(index + offset) % limits[domain.element]!.length]!)) }\nfunction shrinkNumber(value: number, domain: string): number[] { const values: number[] = []; let current = value; while (Math.abs(current) > 1) { current = Math.trunc(current / 2); values.push(current) } values.push(0); return [...new Set(values)].filter(value => ![\"Nat\", \"U8\", \"U32\"].includes(domain) || value >= 0) }\nfunction shrink(value: any, domain: any): any[] { if (typeof domain === \"string\") return typeof value === \"number\" ? shrinkNumber(value, domain) : []; if (domain.kind === \"union\") return domain.members.flatMap((member: any) => typeof member === \"string\" && typeof value === \"number\" ? shrinkNumber(value, member) : []); if (domain.kind === \"specialized\") return domain.values.filter((candidate: any) => candidate !== value).sort((left: any, right: any) => sampleSize([left]) - sampleSize([right])); if (domain.kind === \"record\") return Object.entries(domain.fields).flatMap(([name, field]) => { const omitted = domain.optional?.includes(name) && Object.hasOwn(value, name) ? [Object.fromEntries(Object.entries(value).filter(([key]) => key !== name))] : []; return [...omitted, ...shrink(value[name], field).map(candidate => ({ ...value, [name]: candidate }))] }); if (domain.kind === \"bounded-set\") return [...value.map((_: any, index: number) => value.filter((__: any, at: number) => at !== index)), ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).filter((candidate: number) => !value.some((other: number, at: number) => at !== index && other === candidate)).map((candidate: number) => value.with(index, candidate))), []]; if (domain.kind === \"bounded-map\") return [...value.keys.map((_: any, index: number) => ({ keys: value.keys.filter((__: any, at: number) => at !== index), values: value.values.filter((__: any, at: number) => at !== index) })), ...value.values.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.value).map(candidate => ({ keys: value.keys, values: value.values.with(index, candidate) }))), { keys: [], values: [] }]; const structural: number[][] = []; for (let length = Math.floor(value.length / 2); length > 0; length = Math.floor(length / 2)) structural.push(value.slice(0, length)); if (value.length > 1) structural.push(value.slice(0, 1)); return [...structural, ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).map(candidate => value.with(index, candidate))), []] }\nfunction materialize(value: any, domain: Domain): any { if (typeof domain === \"object\" && domain.kind === \"bounded-array\") return domain.element === \"U8\" ? new Uint8Array(value) : new Uint32Array(value); if (typeof domain === \"object\" && domain.kind === \"bounded-set\") return new Set(value); if (typeof domain === \"object\" && domain.kind === \"bounded-map\") return new Map(value.keys.map((key: number, index: number) => [key, value.values[index]])); return value }\nfunction sampleSize(values: readonly any[]): number { const size = (value: any): number => Array.isArray(value) ? value.length + value.reduce((sum: number, entry: any) => sum + size(entry), 0) : value !== null && typeof value === \"object\" ? Object.values(value).reduce((sum: number, entry) => sum + size(entry), 0) : typeof value === \"number\" ? Math.abs(value) : typeof value === \"string\" ? value.length : Number(value); return values.reduce((total, value) => total + size(value), 0) }\nfunction random(seed: number) { let state = seed >>> 0; return () => ((state = (Math.imul(state, 1664525) + 1013904223) >>> 0) / 0x100000000) }\nfunction samples() { const out: any[][] = replay?.functionName === \"read\" && Array.isArray(replay.arguments) ? [[...replay.arguments]] : []; for (const row of refinementTuples) if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push([...row]); const limit = Math.max(100, out.length); const visit = (at: number, row: any[]) => { if (out.length >= limit) return; if (at === domains.length) { if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push(row); return } for (const value of values(domains[at]!, refinementValues[at])) visit(at + 1, [...row, value]) }; visit(0, []); const next = random(24301); while (out.length < limit) out.push(domains.map((domain, index) => { const candidates = values(domain, refinementValues[index]); return candidates[Math.floor(next() * candidates.length)]! })); return out }\nconst precondition = (x, ok, text) => (x !== -1 && ok === true)\nconst property = (x, ok, text) => { const result = read(x, ok, text); return (result === text) }\n\ntest(\"uneffect property: read\", () => {\n  for (const candidate of samples()) {\n    const invoke = (values: any[]) => values.map((value, index) => materialize(value, domains[index]!))\n    if (!precondition(...invoke(candidate))) continue\n    if (property(...invoke(candidate))) continue\n    const minimal = [...candidate]; for (const joint of refinementTuples.map(row => [...row]).sort((left, right) => sampleSize(left) - sampleSize(right))) { if (sampleSize(joint) < sampleSize(minimal) && precondition(...invoke(joint)) && !property(...invoke(joint))) minimal.splice(0, minimal.length, ...joint) } for (let index = 0; index < minimal.length; index++) for (const value of shrink(minimal[index]!, domains[index]!)) { const next = minimal.with(index, value); if (precondition(...invoke(next)) && !property(...invoke(next))) minimal[index] = value }\n    expect.fail(\"Uneffect counterexample: \" + JSON.stringify({ functionName: \"read\", arguments: minimal, replayed: Boolean(replay && JSON.stringify(candidate) === JSON.stringify(replay.arguments)) }))\n  }\n})\n"
      },
      "boundaries": [
        {
          "fileName": "unions.ts",
          "functionName": "read",
          "generators": [
            {
              "kind": "union",
              "members": [
                {
                  "kind": "literal",
                  "value": -1
                },
                {
                  "kind": "literal",
                  "value": 0
                },
                {
                  "kind": "literal",
                  "value": 2
                }
              ]
            },
            {
              "kind": "union",
              "members": [
                {
                  "kind": "literal",
                  "value": true
                },
                {
                  "kind": "literal",
                  "value": false
                }
              ]
            },
            {
              "kind": "union",
              "members": [
                {
                  "kind": "literal",
                  "value": "😀"
                },
                {
                  "kind": "literal",
                  "value": "good"
                }
              ]
            }
          ],
          "shrinkers": [
            {
              "kind": "union",
              "members": [
                {
                  "kind": "literal",
                  "value": -1
                },
                {
                  "kind": "literal",
                  "value": 0
                },
                {
                  "kind": "literal",
                  "value": 2
                }
              ]
            },
            {
              "kind": "union",
              "members": [
                {
                  "kind": "literal",
                  "value": true
                },
                {
                  "kind": "literal",
                  "value": false
                }
              ]
            },
            {
              "kind": "union",
              "members": [
                {
                  "kind": "literal",
                  "value": "😀"
                },
                {
                  "kind": "literal",
                  "value": "good"
                }
              ]
            }
          ],
          "generatorHints": [
            [],
            [],
            []
          ],
          "generatorTuples": [],
          "requires": [
            "x !== -1 && ok === true"
          ],
          "ensures": [
            "result === text"
          ]
        }
      ],
      "diagnostics": []
    }
  },
  {
    "options": {
      "files": {
        "validate.ts": "export function valid(value: string): boolean { return value.length > 0; }",
        "consumer.ts": "import { valid as check } from \"./validate.js\";\n/* uneffect:requires check(value) */\n/* uneffect:ensures result === value */\nexport function identity(value: string): string { return value; }"
      },
      "predicateSpecializations": {
        "validate.ts:valid": {
          "version": "uneffect-property-predicate/v1",
          "values": [
            "",
            "hello"
          ]
        }
      }
    },
    "expected": {
      "generatedFiles": {
        "consumer.uneffect.test.ts": "// Generated by Uneffect. Test-only code; no production runtime dependency.\nimport { expect, test } from \"vitest\"\nimport { valid as check } from \"./validate.js\"\nimport { identity } from \"./consumer.js\"\n\nconst domains = [{\"kind\":\"specialized\",\"predicate\":\"check\",\"values\":[\"\",\"hello\"]}] as const\nconst refinementValues = [[]] as const\nconst refinementTuples = [] as const\nconst limits: Record<string, readonly number[]> = {\"Int\":[0,1,-1,2,-2,2147483647,-2147483648],\"Nat\":[0,1,2,255,65535],\"U8\":[0,1,2,254,255],\"U32\":[0,1,2,4294967294,4294967295],\"I32\":[0,1,-1,2147483647,-2147483648]}\nconst replay = undefined\ntype Domain = typeof domains[number]\nfunction values(domain: any, refined: readonly any[] = []): any[] { if (typeof domain === \"string\") return [...new Set([...refined, ...limits[domain]!] as any[])]; if (domain.kind === \"union\") return [...new Set([...refined, ...domain.members.flatMap((member: any) => typeof member === \"string\" ? limits[member]! : [member.value])] as any[])]; if (domain.kind === \"specialized\") return [...domain.values]; if (domain.kind === \"record\") { let out: any[] = [{}]; for (const [name, field] of Object.entries(domain.fields)) { const fieldValues = [...(domain.optional?.includes(name) ? [undefined] : []), ...values(field).slice(0, 5)]; out = out.flatMap(record => fieldValues.map(value => value === undefined ? record : ({ ...record, [name]: value }))).slice(0, 64) } return out } if (domain.kind === \"bounded-set\") { const edges = limits[domain.element]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, edges.length) + 1 }, (_, length) => edges.slice(0, length)) } if (domain.kind === \"bounded-map\") { const keys = limits[domain.key]!.slice(0, Math.max(1, domain.maximum)); return Array.from({ length: Math.min(domain.maximum, keys.length) + 1 }, (_, length) => ({ keys: keys.slice(0, length), values: keys.slice(0, length).map((_: number, index: number) => limits[domain.value]![index % limits[domain.value]!.length]!) })) } const maximum = Math.min(domain.maximum, 4096); const lengths = [...new Set([0, Math.min(1, maximum), Math.min(2, maximum), maximum])]; return lengths.map((length, offset) => Array.from({ length }, (_, index) => limits[domain.element]![(index + offset) % limits[domain.element]!.length]!)) }\nfunction shrinkNumber(value: number, domain: string): number[] { const values: number[] = []; let current = value; while (Math.abs(current) > 1) { current = Math.trunc(current / 2); values.push(current) } values.push(0); return [...new Set(values)].filter(value => ![\"Nat\", \"U8\", \"U32\"].includes(domain) || value >= 0) }\nfunction shrink(value: any, domain: any): any[] { if (typeof domain === \"string\") return typeof value === \"number\" ? shrinkNumber(value, domain) : []; if (domain.kind === \"union\") return domain.members.flatMap((member: any) => typeof member === \"string\" && typeof value === \"number\" ? shrinkNumber(value, member) : []); if (domain.kind === \"specialized\") return domain.values.filter((candidate: any) => candidate !== value).sort((left: any, right: any) => sampleSize([left]) - sampleSize([right])); if (domain.kind === \"record\") return Object.entries(domain.fields).flatMap(([name, field]) => { const omitted = domain.optional?.includes(name) && Object.hasOwn(value, name) ? [Object.fromEntries(Object.entries(value).filter(([key]) => key !== name))] : []; return [...omitted, ...shrink(value[name], field).map(candidate => ({ ...value, [name]: candidate }))] }); if (domain.kind === \"bounded-set\") return [...value.map((_: any, index: number) => value.filter((__: any, at: number) => at !== index)), ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).filter((candidate: number) => !value.some((other: number, at: number) => at !== index && other === candidate)).map((candidate: number) => value.with(index, candidate))), []]; if (domain.kind === \"bounded-map\") return [...value.keys.map((_: any, index: number) => ({ keys: value.keys.filter((__: any, at: number) => at !== index), values: value.values.filter((__: any, at: number) => at !== index) })), ...value.values.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.value).map(candidate => ({ keys: value.keys, values: value.values.with(index, candidate) }))), { keys: [], values: [] }]; const structural: number[][] = []; for (let length = Math.floor(value.length / 2); length > 0; length = Math.floor(length / 2)) structural.push(value.slice(0, length)); if (value.length > 1) structural.push(value.slice(0, 1)); return [...structural, ...value.flatMap((entry: number, index: number) => shrinkNumber(entry, domain.element).map(candidate => value.with(index, candidate))), []] }\nfunction materialize(value: any, domain: Domain): any { if (typeof domain === \"object\" && domain.kind === \"bounded-array\") return domain.element === \"U8\" ? new Uint8Array(value) : new Uint32Array(value); if (typeof domain === \"object\" && domain.kind === \"bounded-set\") return new Set(value); if (typeof domain === \"object\" && domain.kind === \"bounded-map\") return new Map(value.keys.map((key: number, index: number) => [key, value.values[index]])); return value }\nfunction sampleSize(values: readonly any[]): number { const size = (value: any): number => Array.isArray(value) ? value.length + value.reduce((sum: number, entry: any) => sum + size(entry), 0) : value !== null && typeof value === \"object\" ? Object.values(value).reduce((sum: number, entry) => sum + size(entry), 0) : typeof value === \"number\" ? Math.abs(value) : typeof value === \"string\" ? value.length : Number(value); return values.reduce((total, value) => total + size(value), 0) }\nfunction random(seed: number) { let state = seed >>> 0; return () => ((state = (Math.imul(state, 1664525) + 1013904223) >>> 0) / 0x100000000) }\nfunction samples() { const out: any[][] = replay?.functionName === \"identity\" && Array.isArray(replay.arguments) ? [[...replay.arguments]] : []; for (const row of refinementTuples) if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push([...row]); const limit = Math.max(100, out.length); const visit = (at: number, row: any[]) => { if (out.length >= limit) return; if (at === domains.length) { if (!out.some(item => JSON.stringify(item) === JSON.stringify(row))) out.push(row); return } for (const value of values(domains[at]!, refinementValues[at])) visit(at + 1, [...row, value]) }; visit(0, []); const next = random(24301); while (out.length < limit) out.push(domains.map((domain, index) => { const candidates = values(domain, refinementValues[index]); return candidates[Math.floor(next() * candidates.length)]! })); return out }\nconst precondition = (value) => (check(value))\nconst property = (value) => { const result = identity(value); return (result === value) }\n\ntest(\"uneffect property: identity\", () => {\n  let tested = 0\n  for (const candidate of samples()) {\n    const invoke = (values: any[]) => values.map((value, index) => materialize(value, domains[index]!))\n    if (!precondition(...invoke(candidate))) continue\n    tested++\n    if (property(...invoke(candidate))) continue\n    const minimal = [...candidate]; for (const joint of refinementTuples.map(row => [...row]).sort((left, right) => sampleSize(left) - sampleSize(right))) { if (sampleSize(joint) < sampleSize(minimal) && precondition(...invoke(joint)) && !property(...invoke(joint))) minimal.splice(0, minimal.length, ...joint) } for (let index = 0; index < minimal.length; index++) for (const value of shrink(minimal[index]!, domains[index]!)) { const next = minimal.with(index, value); if (precondition(...invoke(next)) && !property(...invoke(next))) minimal[index] = value }\n    expect.fail(\"Uneffect counterexample: \" + JSON.stringify({ functionName: \"identity\", arguments: minimal, replayed: Boolean(replay && JSON.stringify(candidate) === JSON.stringify(replay.arguments)) }))\n  }\n  expect(tested, \"Uneffect predicate specialization produced no valid candidates\").toBeGreaterThan(0)\n})\n"
      },
      "boundaries": [
        {
          "fileName": "consumer.ts",
          "functionName": "identity",
          "generators": [
            {
              "kind": "specialized",
              "predicate": "check",
              "values": [
                "",
                "hello"
              ]
            }
          ],
          "shrinkers": [
            {
              "kind": "specialized",
              "predicate": "check",
              "values": [
                "",
                "hello"
              ]
            }
          ],
          "generatorHints": [
            []
          ],
          "generatorTuples": [],
          "requires": [
            "check(value)"
          ],
          "ensures": [
            "result === value"
          ]
        }
      ],
      "diagnostics": []
    }
  }
]
