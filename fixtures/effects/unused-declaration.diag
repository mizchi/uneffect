$ uneffect --evidence fixtures/effects/unused-declaration.ts
# A declaration is an upper bound: declaring more than the body needs is a warning, not an error.

warning effect/unused fixtures/effects/unused-declaration.ts:3 in report
  message: report declares unused effect Fetch
  3 | export function report(value: number) {
    | ^
  declared: Console | Fetch
  inferred: Console
  because: nothing in report or its callees produces Fetch
  hint: delete the declared effect, or keep it deliberately as a wider upper bound; unused declarations never fail the CLI

0 error(s), 1 warning(s)

evidence:
  effects report: Console (verified)
