$ uneffect check --evidence fixtures/effects/scoped-fetch.ts
# A scoped effect is an authority, not a name: the in-scope call verifies, the out-of-scope one does not.

error effect/missing fixtures/effects/scoped-fetch.ts:8 in loadReport
  message: loadReport requires /* uneffect: effect Fetch<GET, "https://reports.example.com/latest"> */
  8 | export function loadReport() {
    | ^
  declared: Fetch<GET, "https://api.example.com/**"> | Net<"api.example.com:443">
  because: loadReport performs fetch("https://reports.example.com/latest") at line 9
  out of authority: the declared Fetch<GET, "https://api.example.com/**"> shares this effect constructor, but its arguments do not cover Fetch<GET, "https://reports.example.com/latest">
  hint: declare the effect in the function's /* uneffect: effect ... */ comment, or move the operation into a callee that already declares it

error effect/missing fixtures/effects/scoped-fetch.ts:8 in loadReport
  message: loadReport requires /* uneffect: effect Net<"reports.example.com:443"> */
  8 | export function loadReport() {
    | ^
  declared: Fetch<GET, "https://api.example.com/**"> | Net<"api.example.com:443">
  because: loadReport performs fetch("https://reports.example.com/latest") at line 9
  out of authority: the declared Net<"api.example.com:443"> shares this effect constructor, but its arguments do not cover Net<"reports.example.com:443">
  hint: declare the effect in the function's /* uneffect: effect ... */ comment, or move the operation into a callee that already declares it

warning effect/unused fixtures/effects/scoped-fetch.ts:8 in loadReport
  message: loadReport declares unused effect Fetch<GET, "https://api.example.com/**">
  8 | export function loadReport() {
    | ^
  declared: Fetch<GET, "https://api.example.com/**"> | Net<"api.example.com:443">
  inferred: Fetch<GET, "https://reports.example.com/latest"> | Net<"reports.example.com:443">
  because: nothing in loadReport or its callees produces Fetch<GET, "https://api.example.com/**">
  hint: delete the declared effect, or keep it deliberately as a wider upper bound; unused declarations never fail the CLI

warning effect/unused fixtures/effects/scoped-fetch.ts:8 in loadReport
  message: loadReport declares unused effect Net<"api.example.com:443">
  8 | export function loadReport() {
    | ^
  declared: Fetch<GET, "https://api.example.com/**"> | Net<"api.example.com:443">
  inferred: Fetch<GET, "https://reports.example.com/latest"> | Net<"reports.example.com:443">
  because: nothing in loadReport or its callees produces Net<"api.example.com:443">
  hint: delete the declared effect, or keep it deliberately as a wider upper bound; unused declarations never fail the CLI

2 error(s), 2 warning(s)

evidence:
  effects loadUsers: Fetch<GET, "https://api.example.com/users"> | Net<"api.example.com:443"> (verified)
  effects loadReport: Fetch<GET, "https://reports.example.com/latest"> | Net<"reports.example.com:443"> (unknown)
