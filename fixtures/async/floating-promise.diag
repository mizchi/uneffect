$ uneffect check --evidence fixtures/async/floating-promise.ts
# A Promise whose rejection nobody observes: an unhandled rejection waiting to happen.

error async/floating-promise fixtures/async/floating-promise.ts:7 in run
  message: run has a floating Promise whose rejection is not observed; await, return, catch, or explicitly void it
  7 |   save("value");
    |   ^
  because: save("value") evaluates to a Promise that no await, return, catch, then-rejection handler, or void expression observes
  hint: await the Promise, return it, attach a rejection handler, or mark the intent with an explicit void

1 error(s), 0 warning(s)

evidence:
  effects save: no effect (inferred)
  effects run: no effect (inferred)
